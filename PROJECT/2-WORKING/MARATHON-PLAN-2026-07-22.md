---
title: Marathon Plan — ranked, freshness-validated, collision-aware queue
status: Active (2-WORKING)
created: 2026-07-22
updated: 2026-07-22
owner: noel
branch: main
doc_type: project
source: ../../ROADMAP.md (open ledger entries)
generated_by: .xyz/utils/marathon-plan.sh
roadmap_exempt: true
goal: >
  A sequenced concurrency plan derived from ROADMAP.md: ranks surviving work by PDDA
  complexity/risk/effort, validates each item is still real, and batches collision-safe
  lanes into waves. Generated — edit the ledger, not this file.
---

<!-- GENERATED by utils/marathon-plan.sh from ROADMAP.md — re-run to refresh; edit the ledger, not this file. -->

# Marathon Plan 2026-07-22 — pre-pre-flight sequenced queue

> Derived from [ROADMAP.md](../../ROADMAP.md) · policy `quick-wins` · weights {eff:2,cx:1,risk:2,dep:3,zone:1} · gh=live.
> The roadmap says **what/why**; this says **what is still real and in what order**. Execution
> detail still lives in each `PROJECT/**` doc — this is a scheduling overlay.

## Status

| What was just completed | What's next |
|---|---|
| Generated by `utils/marathon-plan.sh` on 2026-07-22 from the live ROADMAP ledger (2 items; 2 active across 1 wave(s); 0 held). Drift present: no. | **Wave 1:** #7 ‖ #8. Fire each lane via `swarm-preflight → marathon-drive`, scoped by `ALLOW_PATHS`. Re-run this script when the ledger changes. |

## The one safety rule

Two lanes are safe to run concurrently **iff their write-sets are disjoint** and their zone
caps are respected. Current caps: kernel≤1/wave.

## Collision map

| Zone | Parallel-safe? | Active items here |
|---|---|---|
| kernel | ❌ serialize — one at a time | #8 |
| shim | ✅ one lane per file (serialize when inferred) | — |
| independent | ✅ one lane per file | #7 |

## Per-item scoring

Every input is shown so the ordering is verifiable by hand (lower score = earlier).

| Item | cx | risk | eff | zone | deps | score | wave |
|---|---|---|---|---|---|---|---|
| [#7] GH-7 — front-door: FRONTDOOR.md has no staleness/re-audit trigger or maturity-stage field | 2 | 1 | 3 | independent | — | 10 | 1 |
| [#8] GH-8 — shakedown: no re-run trigger when the audited skill's own script layout changes | 3 | 1 | 3 | kernel | — | 13 | 1 |

`*` = zone inferred from keywords (no preflight contract write-set to prove it).

## Recommended waves

**Wave 1:** #7 ‖ #8

- #7 → suggested_branch: `marathon/gh-7-frontdoor-staleness-maturity-stage-2026-07-22`
- #8 → suggested_branch: `marathon/gh-8-shakedown-rerun-trigger-on-layout-change-2026-07-22`

## Contract seams — pin a contract before launching (GH-5)

_None — no two same-wave lanes share a directory spine (deeper than a top-level dir)._

## Held / flagged — excluded from active waves

## How to fire a lane

Per lane, the existing pipeline applies — no new control plane:

```
.xyz/utils/swarm-preflight.sh --project-doc <PROJECT/**/doc.md>   # or --gh-issue N
   → ready packet (candidate/freshness/fix-still-required + lane assignment)
.xyz/relay-automation/marathon-drive.sh ...   # build→gate→review, contained
```

- **Lane scoping:** give each lane an `ALLOW_PATHS` matching only its zone above.
- If the lane's allowlist includes filesystem-touching `test/*.sh`, treat those tests as read-only specs in-turn; the outer harness gate verifies them after the turn, outside the isolated worktree.

---

*Generated from [ROADMAP.md](../../ROADMAP.md) (source of truth). Re-run `.xyz/utils/marathon-plan.sh` after editing the ledger.*
